An simple adduser script.

Ubuntu : Install g++ compiler with : apt install gcc g++ -y
CentOS : Install g++ compiler with : yum install gcc g++ -y

To compile : g++ adduser.cpp -o adduserscript
 
